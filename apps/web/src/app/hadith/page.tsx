import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function HadithPage() {
  return (
    <PlaceholderPage
      title="Hadith"
      kicker="Source on every result"
      body="Bukhari, Muslim, and other collections need a reviewed import. This screen stays empty rather than displaying unverified narrations."
    />
  );
}
