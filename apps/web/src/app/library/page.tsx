import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function LibraryPage() {
  return (
    <PlaceholderPage
      title="Library"
      kicker="Versioned knowledge"
      body="Tafsir, seerah, and fiqh texts must carry author, edition, license, and version before they are readable here."
    />
  );
}
