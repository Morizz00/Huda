import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function BookmarksPage() {
  return (
    <PlaceholderPage
      title="Bookmarks"
      kicker="After you have an account"
      body="Ayah bookmarks, collections, and notes sync when authentication exists. Nothing is stored in the cloud from this screen yet."
    />
  );
}
