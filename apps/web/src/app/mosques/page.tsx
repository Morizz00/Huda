import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function MosquesPage() {
  return (
    <PlaceholderPage
      title="Mosques"
      kicker="Nearby, when maps are wired"
      body="A geospatial index and a map provider are required before listing masajid. HUDA will not fake pins or scrape unverified directories."
    />
  );
}
