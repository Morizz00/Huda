import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function DuasPage() {
  return (
    <PlaceholderPage
      title="Duas"
      kicker="Every dua needs a source"
      body="Collections will list Arabic, translation, and a reference (Qur’an or hadith). HUDA will not invent supplications or fill this page with unsourced text."
    />
  );
}
