import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function AdhkarPage() {
  return (
    <PlaceholderPage
      title="Adhkar"
      kicker="Morning, evening, and after prayer"
      body="Ordered sessions with repeat counts will arrive with reviewed collections. Nothing is shown here until those texts are imported with provenance."
    />
  );
}
