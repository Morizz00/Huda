import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function LearnPage() {
  return (
    <PlaceholderPage
      title="Learn"
      kicker="Paths, not a feed"
      body="Structured lessons will sit on top of the library. Until then there is no generated curriculum."
    />
  );
}
