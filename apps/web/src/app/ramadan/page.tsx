import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function RamadanPage() {
  return (
    <PlaceholderPage
      title="Ramadan"
      kicker="A quieter month mode"
      body="Suhoor, iftar, khatm, and nightly extras will activate from the Hijri calendar. Fasting trackers will never be public or ranked."
    />
  );
}
